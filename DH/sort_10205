package algoJava;

import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Scanner;
import java.util.stream.IntStream;

public class Main_10205 {
    public static int sum;
    public static int hydraHeadCnt;

    public static void main(String[] args) throws IOException {
        Scanner sc = new Scanner(System.in);
        int k = Integer.parseInt( sc.nextLine().trim() ); // 데이터 집합 개

        //for( int i = 0; i < k; i++ ){
            int hydraHeadCnt = Integer.parseInt( sc.nextLine().trim());
            Arrays.stream(sc.nextLine().trim().split("")).forEach(
                    item -> {
                        sum = hydraHeadCnt + ( item.equals("c") ? 1 : -1 );
                    }
            );

        //rstList.stream().

        //};

    }
}
